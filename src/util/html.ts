export function getHtmlPath(templateName: string) {
  return `./dist/${templateName}.html`;
}

export function getHtmlPreviewPath(templateName: string) {
  return `./dist/${templateName}-preview.html`;
}
